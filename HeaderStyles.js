import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  header: {
    padding: 40,
    fontSize: 30,
    textAlign: 'center',
    fontWeight: '600',
    color: 'tomato',
  },
});
